<?php

/**
 * implements hook theme().
 */
function custom_service_theme($existing, $type, $theme, $path)
{
    return [
        'custom_service' => [
            'variables' => [
                'data' => ['data' => NULL],
            ],
        ],

    ];
}
